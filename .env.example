# MongoDB
MONGO_URL=mongodb://localhost:27017
DB_NAME=liquiditybars

# Server
PORT=8002
NODE_ENV=development
JWT_SECRET=change-me-in-prod
JWT_EXPIRES_IN=7d

# MySQL (for sql-mirror sync) — run "npm run sync:sql" to pull data into Mongo
SQL_HOST=localhost
SQL_PORT=3306
SQL_USER=liquiditybars
SQL_PASSWORD=.R)NUvfwkT81
SQL_DATABASE=liquiditybars
